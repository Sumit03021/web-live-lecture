const dog=require('./dog')
const cat=require('./cat')

module.exports={
     dog,cat
}

//whenever any folder exported then index.js file responsible for exporting the folder otherwise shows errors








